kind = "mobile"
previewPath = "/mobile/"
title = "Lumière Mobile"
version = "1.0.0"
id = "artifacts/salon-app-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 21452

[services.development]
run = "pnpm --filter @workspace/salon-app-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/salon-app-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/salon-app-mobile", "run", "serve" ]

[services.env]
PORT = "21452"
BASE_PATH = "/mobile/"
